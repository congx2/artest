
<axr-tracker-2dmarker
    marker="{{marker}}"
    gltf="{{gltf}}"
    width="{{renderWidth}}"
    height="{{renderHeight}}"
    style="width:{{width}}px;height:{{height}}px;top:0;left:0;display:block;"
/>
